// Optional backend for YouTube/Vimeo downloads (see backend/ in the repo).
// Leave empty to disable the YouTube tab's server-assisted download: the UI
// will explain how to download the video manually instead.
export const BACKEND_URL = 'https://episuarez-edadplay-backend.hf.space';
